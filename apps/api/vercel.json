{
  "version": 2,
  "buildCommand": "echo 'No build needed for edge functions'",
  "functions": {
    "api/**/*.ts": {
      "runtime": "edge"
    }
  },
  "rewrites": [
    { "source": "/v1/(.*)", "destination": "/api/index" }
  ]
}
